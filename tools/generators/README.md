# Workspace generators

This directory is reserved for generators that encode Rusty D20's established
Nx boundaries. Do not add an ad hoc generator until repeated product work proves
the shape.

Candidates include feature libraries with live-scenario stubs, presentational
components, store slices, and platform ports. Every generated library must have
one type tag, one scope tag, and one package-root public barrel.
